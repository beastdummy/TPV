# URL del TPV (Better Auth, ventas, admin). Sin barra final.
# Desarrollo: mismo PC, web en 3001 y TPV en 3000
VITE_TPV_APP_URL=http://localhost:3000

# Opcional: ruta de registro en el TPV (por defecto /login mientras no exista /register)
# VITE_TPV_REGISTER_PATH=/login
